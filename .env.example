# Copy to `.env`. Set `VITE_*` on Vercel for production builds.

# --- Frontend (Vite) ---
VITE_BACKEND_URL=http://localhost:5000
VITE_CREDIT_API_URL=http://localhost:8000
# Legacy: full base including /api
# VITE_API_URL=http://localhost:5000/api

# --- Often used by backend / scripts (see SuperBazaar_backend/superbazaar-backend/.env) ---
# PORT=5000
# NODE_ENV=development
# MONGODB_URI=
# JWT_SECRET=
# JWT_EXPIRE=24h
# FASTAPI_URL=http://localhost:8000
