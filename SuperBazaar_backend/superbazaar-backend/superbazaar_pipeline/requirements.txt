# Pin to the environment used when saving xgb_m.pkl / xgb_c.pkl / scaler_*.pkl / calibration.pkl.
# Install in the same venv as your FastAPI scoring app (main.py).
xgboost==3.2.0
scikit-learn==1.6.1
numpy==2.2.5
pandas==2.2.3
